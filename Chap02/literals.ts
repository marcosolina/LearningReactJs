/*
In questo caso scrivo fisso a programma
i valori che accetto. Una sorta di enum
*/

let literal: "uno" | "due" | "tre" | "quattro";

literal = "uno"; // Ok
//literal = "xxx"; // Compile error

console.log(literal);
